// import { store } from "./store";

// // import { saveCart } from "./features/cart/cartStorage";
// import { persist } from "./features/cart/cartStorage";

// let currentCart = store.getState().cart;

// store.subscribe(() => {
//   const nextCart = store.getState().cart;

//   if (currentCart !== nextCart) {
//     currentCart = nextCart;

//     persist(nextCart);
//   }
// });
